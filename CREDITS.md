# Credits

## Images

The images in `public/` are project work supplied by the repository owner.
They are **not** covered by this repository's MIT license — the projects,
brands and visuals belong to their respective owners. Do not reuse them; swap
in your own work via the `PROJECTS` list in `lib/carousel/config.js`.

The captions, service tags and stat figures attached to each image in that
list are placeholder copy, not verified reported figures.

## Code

Everything else — the engine, the shader, the React wrapper — is original and
MIT-licensed (see `LICENSE`).
